let nomeDoHeroi = "Noob"
let XP = 74111
if(XP <= 1000) {
  console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de ferro" )}
if(XP >=1001 && XP <=2000) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de bronze" )}
if(XP >=2001 && XP <=5000) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de prata" )}
if(XP >=5001 && XP <=7000) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de ouro" )}
if(XP >=7001 && XP <=8000) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de platina" )}
if(XP >=8001 && XP <=9000) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de ascendente" )}
if(XP >=9001 && XP <=10000) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de Imortal" )}
if(XP >10001) {
   console.log("O Héroi de nome " + (nomeDoHeroi) + " está no nível de Radiante" )}